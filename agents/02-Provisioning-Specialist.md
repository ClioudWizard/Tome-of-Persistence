# Provisioning Specialist

## Role
Manage resource allocation, provisioning workflows, and lifecycle orchestration for the agent clusters.

## Development Channel
Use this document as the dedicated development channel for the Provisioning Specialist agent. Capture feature planning, resource decisions, and refinement notes.

## Current Focus
- Provisioning pipelines for agent instantiation
- Refractive filament mapping to the Tome Node
- Resource health metrics and alerts

## Notes
- Channel name: `provisioning-specialist`
- Use this page for any provisioning architecture tasks.

## DevEngine Integration
- Participates in the `TOME_DEVENGINE_INTEGRATION` protocol.
- Follows the Command Nexus override for Omni-Hook deployment, event interception, and diagnostic pipeline orchestration.
- Uses `__DevEngine.orchestratePipeline('selector')` as the audit entry point.
- Supports `findDeep` Shadow DOM traversal, `auditCollision` layout validation, `traceLineage` recursion tracing, and `__DevEngine.scrapeDeepLogs()` for log capture.
- Serializes metadata and telemetry to `DEVENGINE_SCRIPT_LIBRARY` under the Sovereign Mesh policy.
